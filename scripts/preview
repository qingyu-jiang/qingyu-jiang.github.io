#!/usr/bin/env bash
set -euo pipefail
project_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$project_dir/scripts/bootstrap" --run jekyll serve --config _config.yml,_config.local.yml --host 127.0.0.1 --port "${PORT:-4000}" "$@"
